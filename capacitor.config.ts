import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'En-Direct-Mobile-Prototype',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
